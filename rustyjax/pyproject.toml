[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[tool.maturin]
python-source = "python"
module-name = "rustyjax._core"

[project]
name = "rustyjax"
version = "0.1.0"
requires-python = ">=3.10"
